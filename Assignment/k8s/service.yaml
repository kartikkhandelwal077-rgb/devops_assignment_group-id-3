apiVersion: v1
kind: Service
metadata:
  name: devops-service
  namespace: devops-prod
spec:
  type: LoadBalancer
  selector:
    app: devops-microservice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
